interface:
  display_name: "AUTOCARE Agent System Bootstrap"
  short_description: "Opt into role-aware queued work without background automation."
  default_prompt: "Activate the opt-in AUTOCARE role workflow for my selected or queued task. Preserve unrelated changes, use one Codex session by default, report an empty queue honestly, and spawn subagents only when I explicitly request parallel work."
