[build]
builder = "RAILPACK"
buildCommand = "npm --workspace backend run build:ecommerce"
watchPatterns = [
  "/backend/**",
  "/packages/**",
  "/package.json",
  "/package-lock.json",
]

[deploy]
startCommand = "npm --workspace backend run start:ecommerce"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
