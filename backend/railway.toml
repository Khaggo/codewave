[build]
builder = "RAILPACK"
buildCommand = "npm --workspace backend run build:main"
watchPatterns = [
  "/backend/**",
  "/packages/**",
  "/package.json",
  "/package-lock.json",
]

[deploy]
preDeployCommand = "npm --workspace backend run db:migrate"
startCommand = "npm --workspace backend run start:main:compiled"
healthcheckPath = "/api/health/ready"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
