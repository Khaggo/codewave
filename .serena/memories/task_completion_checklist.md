Use the smallest relevant checks first, then broaden with risk. Backend changes: run focused Jest tests, then `npm run build`; use `npm test -- --runInBand` for shared/security/cross-module changes. Architecture docs: run `cd backend && npm run docs:validate` and refresh validator-owned hashes in `docs/architecture/agent-manifest.json` for changed canonical files. Frontend changes: run targeted ESLint, all `frontend/src/**/*.test.mjs` from the repository root when shared UI/contracts change, and an isolated build such as `$env:NEXT_DIST_DIR='.next-check'; npm run build`. Mobile changes: run all `mobile/src/**/*.test.mjs` with `node --test`, then Expo export for bundling changes. API changes: compare live Swagger `/docs-json` with DTOs and frontend/mobile clients. Runtime ports are main 3000, ecommerce 3001, staff web 3002, Expo LAN 8081, and Expo web 8090; check listeners and health before starting anything. Browser-facing work should include console/network checks and desktop/mobile visual review. Production dependency audit targets are zero for root/frontend/backend; do not force breaking Expo or toolchain upgrades to silence dev/transitive advisories. Preserve unrelated dirty-worktree changes.